package com.ke.dawaati.widgets.reader.adapter

class PdfScale {
    var scale = DEFAULT_SCALE
    var centerX = 0f
    var centerY = 0f

    companion object {
        const val DEFAULT_SCALE = 1.0f
    }
}
